import { FormDateTimeField } from './form-date-time-field';
import { FormTextField } from './form-text-field';
import { FormPicturField } from './picture-field';

export const Form = {
  TextField: FormTextField,
  DateTimeField: FormDateTimeField,
  PictureField: FormPicturField,
};
